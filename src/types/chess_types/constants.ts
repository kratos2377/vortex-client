export enum Color {
    WHITE = "White",
    BLACK = "Black",
  }

  
  
  export enum ChessStates {
    STARTING = "STARTING",
    IN_PROGRESS = "IN PROGRESS",
    OVER = "OVER",
    READY = "READY",
    NOT_STARTED = "NOT STARTED"
  }